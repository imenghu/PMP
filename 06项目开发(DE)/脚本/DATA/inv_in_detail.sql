truncate table inv_in_detail
set IDENTITY_INSERT inv_in_detail  ON
go
insert INTO inv_in_detail (in_detail_id,
in_detail_type,
arrival_notice_id,
pur_task_id,
mat_refund_id,
org_id,
mat_id,
mat_code,
mat_name,
mat_spesc,
vendor_id,
vendor_code,
vendor_name,
plan_in_slnum,
deduction_slnum,
in_slnum,
in_slnum_unit,
in_stnum,
in_stnum_unit,
sl_st_scale,
if_tests,
depot_id,
depot_name,
if_compromise,
submit_userid,
in_remarks,
state,
create_time,
update_time) select *
FROM openquery(MYSQL, 'SELECT in_detail_id,
in_detail_type,
arrival_notice_id,
pur_task_id,
mat_refund_id,
org_id,
mat_id,
mat_code,
mat_name,
mat_spesc,
vendor_id,
vendor_code,
vendor_name,
plan_in_slnum,
deduction_slnum,
in_slnum,
in_slnum_unit,
in_stnum,
in_stnum_unit,
sl_st_scale,
if_tests,
depot_id,
depot_name,
cast(if_compromise as char(1)) as if_compromise,
submit_userid,
in_remarks,
cast(state as char(255)) as state,
create_time,
update_time FROM info_sys.inv_in_detail') 
set IDENTITY_INSERT inv_in_detail  OFF
go